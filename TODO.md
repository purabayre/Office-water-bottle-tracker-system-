# TODO - Fix project structure and make it working

- [x] Add backend `package.json` under `src/backend`
- [x] Update `src/frontend/src/api.js` to use `VITE_API_BASE_URL`
- [x] Create `src/frontend/.env.example`
- [x] Create `src/backend/.env.example`
- [x] Install dependencies and run both services (frontend installed, backend installed; backend runs but needs MONGO_URI)
- [ ] Start backend with a real MongoDB `MONGO_URI` (required)
- [ ] Verify UI calls: `/api/entries/month`, `/api/price/current`, `/api/pdf/monthly`
